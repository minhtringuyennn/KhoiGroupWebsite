---
interface Props {
  date: string;
  text: string;
}
const { date, text } = Astro.props;
---
<li class="flex gap-3 text-sm">
  <span class="text-gray-400 font-mono flex-shrink-0 w-16 text-xs pt-0.5">{date}</span>
  <span class="text-gray-700">{text}</span>
</li>
